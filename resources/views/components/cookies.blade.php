{{--
    Global floating UI mount.
    This component is rendered once by the shared public layout, so the
    promotional CTA is available across public pages without duplication.
--}}
<x-sell-faster-floating-cta />
